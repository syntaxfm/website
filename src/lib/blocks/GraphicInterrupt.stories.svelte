<script module>
	import { defineMeta } from '@storybook/addon-svelte-csf';
	import GraphicInterrupt from './GraphicInterrupt.svelte';

	const { Story } = defineMeta({
		title: 'Blocks/GraphicInterrupt',
		component: GraphicInterrupt
	});
</script>

<Story name="Default" />
