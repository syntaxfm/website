<script module>
	import { defineMeta } from '@storybook/addon-svelte-csf';
	import PlatformLinks from './PlatformLinks.svelte';

	const { Story } = defineMeta({
		title: 'Layout/PlatformLinks',
		component: PlatformLinks
	});
</script>

<Story name="Default" />
